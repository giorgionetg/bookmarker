<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a href="{{ path("bookmarker_index") }}" class="navbar-brand">Bookmarker</a>
                </div>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav">
                        <li><a href="{{ path("bookmarker_feed") }}">All bookmarks</a></li>
                        <li><a href="{{ path("bookmarker_me") }}">My bookmarks</a></li>
                        <li><a href="{{ path("bookmarker_create") }}">Add a bookmark</a></li>
                        <li><a href="{{ path("bookmarker_popular") }}">What's popular</a></li>
                    </ul>
                    {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                        <ul class="nav navbar-nav navbar-right">
                            <li><a href="{{ path("logout") }}">Sign out</a></li>
                        </ul>
                        <p class="navbar-text pull-right">Signed in as <strong>{{ app.user.username }}</strong></p>
                    {% else %}
                        <ul class="nav navbar-nav navbar-right">
                            <li><a href="{{ path("login") }}">Sign in</a></li>
                        </ul>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</nav>
